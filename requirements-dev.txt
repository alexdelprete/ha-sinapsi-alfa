# Dev dependencies (excluding pytest-homeassistant-custom-component which is installed separately)
# pytest-homeassistant-custom-component is installed first, then these are layered on top
colorlog>=6.10.0
getmac>=0.9.5
homeassistant>=2025.10.0
homeassistant-stubs
mdformat
mdformat-gfm
modbuslink>=1.4.1
pre-commit
pymarkdownlnt
ruff>=0.8.0
ty
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov
pytest-timeout
